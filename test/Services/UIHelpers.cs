namespace ChessDroid.Services
{
    /// <summary>
    /// UI Helper functions for ChessDroid
    /// Tooltips, visual feedback, and user interface utilities
    /// Note: Main settings dialog lives in SettingsForm.cs
    /// </summary>
    public static class UIHelpers
    {
        // All methods in this class were found to be unused (dead code) and have been removed:
        // - CreateQuickInfoPanel: never called
        // - AddTooltip: never called (also had ToolTip memory leak)
        // - CreateHelpButton: never called
        // - ShowAnalysisProgress: never called
        // - FlashControl: never called
        //
        // This class is retained as a placeholder for future UI helper methods.
        // If any of the removed functionality is needed, it should be re-implemented
        // with proper resource management.
    }
}
